name: gen_named_pipe_python_checker

on:
  push:
    branches: [ master ]
    paths:
      - 'gen_named_pipe/**/*.py'
      - 'setup.py'
  pull_request:
    branches: [ master ]
    paths:
      - 'gen_named_pipe/**/*.py'
      - 'setup.py'

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true

      - name: Check module length and line limits
        run: |
          python3 gates/gates/limits_checker.py gen_named_pipe
