name: gen_named_pipe_toc
on:
  push:
    branches: [ master ]
    paths:
      - '**.md'
jobs:
  generateTOC:
    name: gen_named_pipe_toc
    runs-on: ubuntu-latest
    steps:
      - uses: technote-space/toc-generator@v4